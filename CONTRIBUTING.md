# Contributing

## Development

```bash
npm install
npm run check
```

Use focused tests for behavior changes:

```bash
npm run test
```

## Pull Requests

- Keep changes scoped to one concern.
- Update README, docs, and examples when public API or CLI behavior changes.
- Run `npm run check` before requesting review.
- Use Conventional Commit style for commit messages.
